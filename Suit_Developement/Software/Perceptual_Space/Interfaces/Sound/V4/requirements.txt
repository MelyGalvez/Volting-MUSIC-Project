# Runtime (both pure Python):
mido>=1.3        # MIDI file parsing (load time only)
requests>=2.28   # ESP32 HTTP polling

# MIDI output uses the built-in Windows Multimedia API
# (ctypes) — no package needed on Windows. On macOS/Linux
# install the optional pygame backend:
#   pip install pygame

# Development:
#   pip install pytest
